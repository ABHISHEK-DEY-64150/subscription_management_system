<!DOCTYPE html>
<html>
  <head>
    <title>Ispmanager</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <%= csrf_meta_tags %> <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/b99e675b6e.js"></script>
   
  </head>

  <body>

    <%  if Currentcustomer.customer.nil? &&  Current.provider.nil?%> 
    <%= yield %> 
    <% elsif Currentcustomer.customer.nil? %>
    <%= render partial: "layouts/sidebarprovider" %> 
    <% else %> 
    <%= render partial: "layouts/sidebar" %> 
    <% end %>

  </body>
</html>