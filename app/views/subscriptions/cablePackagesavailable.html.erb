<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag "cablepackageavailable", "data-turbo-track": "reload" %>
  <title>Document</title>
</head>
<body>
  <h1>Cable Packages available</h1>

<% if @mysub.present? && @mysub.errors.any? %>
<% @mysub.errors.full_messages.each do |message|%>
  <%= message %>
<% end %>
<% end %>
<table id="cable_package">
  <thead>
    <tr>
      <th>description</th>
      <th>price</th>
      <th>servicetype</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @cablePackage.each do |pack| %>
      <% if flash[:subscription_errors] %>
      <div style="color:red"><%= flash[:subscription_errors] %></div>
      <div ><%= flash[:subscription_errors] = "" %></div>
      <% end %>
      <tr>
        <td><%= pack.description %></td>
        <td><%= pack.price %></td>
        <td><%= pack.servicetype %></td>

        <td>       
            <%= button_to "Subscribe", addMysubscription_path(pack), method: :post %>
        </td>

      </tr>
    <% end %>
  </tbody>
</table>

</body>
</html>










